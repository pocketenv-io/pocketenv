amends "../../schema/lexicon.pkl"

lexicon = 1
id = "io.pocketenv.secret.getSecret"
defs = new Mapping<String, Query> {
  ["main"] {
    type = "query"
    parameters {
      type = "params"
      required = List("id")
      properties {
        ["id"] = new StringType {
          type = "string"
          description = "The ID of the secret for which to retrieve secrets."
        }
      }
    }
    output {
      encoding = "application/json"
      schema = new ObjectType {
        type = "object"
        properties {
          ["secret"] = new Ref {
            ref = "io.pocketenv.secret.defs#secretView"
          }
        }
      }
    }
  }
}
