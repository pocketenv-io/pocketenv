amends "../../schema/lexicon.pkl"

lexicon = 1
id = "io.pocketenv.volume.getVolume"
defs = new Mapping<String, Query> {
  ["main"] {
    type = "query"
    parameters {
      type = "params"
      required = List("id")
      properties {
        ["id"] = new StringType {
          type = "string"
          description = "The ID of the volume for which to retrieve volumes."
        }
      }
    }
    output {
      encoding = "application/json"
      schema = new ObjectType {
        type = "object"
        properties {
          ["volume"] = new Ref {
            ref = "io.pocketenv.volume.defs#volumeView"
          }
        }
      }
    }
  }
}
